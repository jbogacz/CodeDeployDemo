version: 0.0
os: linux
files:
  - source: /
    destination: /tmp/codedeploy-deployment-staging-area/
hooks:
  ApplicationStart:
    - location: application_start.sh
      timeout: 300
      runas: root